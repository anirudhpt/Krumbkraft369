import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "KrumbKraft - Artisanal Sourdough & Fresh Breads",
  description: "Experience the finest handcrafted sourdough breads, baked fresh daily with traditional techniques and premium ingredients. Order seamlessly through WhatsApp.",
  keywords: "bakery, sourdough, artisanal bread, fresh baked, WhatsApp ordering, KrumbKraft",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
        suppressHydrationWarning={true}
      >
        {children}
      </body>
    </html>
  );
}
