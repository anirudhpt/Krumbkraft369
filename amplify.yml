version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 20.15.1
        - nvm use 20.15.1
        - npm ci --cache .npm --prefer-offline
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - .npm/**/*
      - .next/cache/**/*
      - node_modules/**/*
